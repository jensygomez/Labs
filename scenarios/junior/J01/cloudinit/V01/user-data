#cloud-config

runcmd:
  # ❌ Error clásico de junior
  - setenforce 0

  # ❌ Mala práctica: NO persistente pero peligrosa
  - echo "SELinux was disabled at runtime (bad practice)" > /etc/j01-variant-v01.flag
