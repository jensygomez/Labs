# profile-lab.yml
config:
  user.user-data: |
    #cloud-config
    package_update: true
    packages:
      - iputils-ping
      - net-tools
      - iproute2
      - curl
      - wget
      - openssh-server
      - python3       # Necesario para que Ansible funcione después
      - python3-pip
    ssh_authorized_keys:
      # Pega aquí tu clave pública (cat ~/.ssh/id_rsa.pub) para entrar sin password
      - ssh-rsa ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAAIKWfbAiQX1GhxiPrlZwpfISA7Ni+4b4kVyqQ8ktrC6Yh jensyg@golden-pilot-lab
    users:
      - default
      - name: admin
        sudo: ALL=(ALL) NOPASSWD:ALL
        groups: users, admin
        shell: /bin/bash

description: Perfil base barebone para los 10 servidores del laboratorio
devices:
  eth0:
    name: eth0
    nictype: bridged
    parent: lab-net
    type: nic
  root:
    path: /
    pool: default
    type: disk
name: profile-lab
