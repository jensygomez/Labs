-- SYSTECH-HA-001 - Fase 06 - Seeding de datos de prueba
-- Generado por Ansible - no editar a mano en db01

CREATE TABLE IF NOT EXISTS {{ db_seed_table }} (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(100) NOT NULL,
    email VARCHAR(100) NOT NULL,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);

INSERT IGNORE INTO {{ db_seed_table }} (id, name, email) VALUES
    (1, 'ACME Test Customer 1', 'customer1@acme.test'),
    (2, 'ACME Test Customer 2', 'customer2@acme.test'),
    (3, 'ACME Test Customer 3', 'customer3@acme.test');
