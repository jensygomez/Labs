---
# ==============================================================================
# LOAD BALANCER SETUP — AlmaLinux 9 (LXC en Proxmox)
# ==============================================================================

- name: "Instalar repositorio EPEL y herramientas"
  ansible.builtin.dnf:
    name:
      - epel-release
    state: present
  when: ansible_os_family == "RedHat"

- name: "Instalar HAProxy, Keepalived y Firewalld"
  ansible.builtin.dnf:
    name:
      - haproxy
      - keepalived
      - psmisc
      - firewalld
    state: present
    update_cache: true
  when: ansible_os_family == "RedHat"

- name: "Habilitar asignación de IP no local en el Kernel (ip_nonlocal_bind)"
  ansible.posix.sysctl:
    name: net.ipv4.ip_nonlocal_bind
    value: '1'
    state: present
    reload: true

# --- FIREWALLD (Para gestionar puertos dentro del LXC) ---

- name: "Asegurar servicio Firewalld activo"
  ansible.builtin.systemd:
    name: firewalld
    state: started
    enabled: true
  when: ansible_os_family == "RedHat"

- name: "Permitir servicios HTTP y HTTPS en Firewalld"
  ansible.posix.firewalld:
    service: "{{ item }}"
    permanent: true
    state: enabled
    immediate: true
  loop:
    - http
    - https
  when: ansible_os_family == "RedHat"

- name: "Permitir protocolo VRRP (Keepalived) en Firewalld"
  ansible.posix.firewalld:
    rich_rule: 'rule protocol value="vrrp" accept'
    permanent: true
    state: enabled
    immediate: true
  when: ansible_os_family == "RedHat"

# --- CONFIGURACIÓN DE SERVICIOS ---

- name: "Desplegar configuración de Keepalived desde plantilla Jinja2"
  ansible.builtin.template:
    src: keepalived.conf.j2
    dest: /etc/keepalived/keepalived.conf
    owner: root
    group: root
    mode: '0644'
  notify: "Reiniciar Keepalived"

- name: "Desplegar configuración de HAProxy desde plantilla Jinja2"
  ansible.builtin.template:
    src: haproxy.cfg.j2
    dest: /etc/haproxy/haproxy.cfg
    owner: root
    group: root
    mode: '0644'
  notify: "Reiniciar HAProxy"

- name: "Garantizar que Keepalived y HAProxy estén iniciados y habilitados"
  ansible.builtin.systemd:
    name: "{{ item }}"
    state: started
    enabled: true
  loop:
    - keepalived
    - haproxy
  when: ansible_os_family == "RedHat"
