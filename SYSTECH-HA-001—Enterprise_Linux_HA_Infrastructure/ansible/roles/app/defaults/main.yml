---
app_port: 8000
app_install_dir: /opt/acme_app
app_system_user: app_svc
app_venv_path: "{{ app_install_dir }}/venv"
#Datos de conexion a la DB (Host resuelto dinamicamente via inventario)
app_db_host: "{{ hostvars['db01']['ansible_host'] }}"
app_db_name: "acme_db"
app_db_user: "app_user"
app_db_password: "{{ vault_app_db_password }}"
