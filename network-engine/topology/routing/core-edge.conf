# ------------------------------------------------------------------------------
# ROUTING - CORE-EDGE
# ------------------------------------------------------------------------------

ROUTES+=(
  # SOLO esta ruta es necesaria:
  "$NS_CORE_EDGE:default:10.255.255.1"  # Hacia EDGE-1 (internet)
  
  # Las rutas a VLANs se crean AUTOMÁTICAMENTE (connected routes)
  # NO añadir: 10.10.0.0/24, 10.20.0.0/24, etc.
)