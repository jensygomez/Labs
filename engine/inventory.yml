# inventory.yml modificado:
all:
  children:
    admin:
      hosts:
        admin01:
          ansible_host: 192.168.122.20

    web:
      hosts:
        web01:
          ansible_host: 192.168.122.22

    client:
      hosts:
        client01:
          ansible_host: 192.168.122.21

    # AÑADE un grupo 'controller' o añade localhost a todos los grupos
    controller:
      hosts:
        localhost:
          ansible_connection: local

  vars:
    ansible_user: student
    ansible_python_interpreter: /usr/bin/python3
    ansible_ssh_private_key_file: /home/jensy/GitHub/Labs/.ssh/id_rhcsalabs